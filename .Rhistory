group_by(seq_ID) |>
# create alternating sequence
mutate(event_ab = rep_len(c("a","b"), length.out = n())) |>
# only keep columns needed for sequence analysis
select(-starts_with("TOL"),
-starts_with("OL"),
-Notes) |>
merge(intervals_cat, by = "datetime", keep = FALSE)
selns_seq <- intervals_cat |>
# get rid of rows without selection info
filter(!is.na(Selection),
seq_ID != "NA_NA") |>
# group by line-activity combo
group_by(seq_ID) |>
# create alternating sequence
mutate(event_ab = rep_len(c("a","b"), length.out = n())) |>
# only keep columns needed for sequence analysis
select(-starts_with("TOL"),
-starts_with("OL"),
-Notes) |>
full_join(intervals_cat, by = "datetime", keep = FALSE)
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_")))
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times in selection table
filter(seq_ID != "NA_NA") |>
# group by selection to create alternating sequence
group_by(Selection) |>
mutate(event_ab = rep_len(c("a","b"), length.out = n()))
View(intervals_cat)
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times in selection table
filter(seq_ID != "NA_NA") |>
# group by sequence ID to create alternating sequence by selection
group_by(seq_ID, Selection) |>
mutate(event_ab = rep.int(c("a","b"), times = n())
)
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times in selection table
filter(seq_ID != "NA_NA") |>
# group by sequence ID to create alternating sequence by selection
group_by(seq_ID) |>
mutate(event_ab = rep.int(c("a","b"), times = n())
)
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times in selection table
filter(seq_ID != "NA_NA") |>
# group by sequence ID to create alternating sequence by selection
group_by(seq_ID) |>
mutate(event_ab = rep.int(c("a","b"))
)
rep(1:4, each = 2, times = 3)
x <- factor(LETTERS[1:4]); names(x) <- letters[1:4]
x
rep.int(x, 2)
rep.int(c("a","b"), times = 3)
rep.int(c("a","b"), each = 3, times = 3)
rep(c("a","b"), each = 3, times = 3)
rep(c("a","b"), each = 3)
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times in selection table
filter(seq_ID != "NA_NA") |>
# group by sequence ID to create alternating sequence by selection
group_by(seq_ID) |>
mutate(event_ab = rep(c("a","b"), each = n())
)
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times in selection table
filter(seq_ID != "NA_NA") |>
# group by sequence ID to create alternating sequence by selection
group_by(seq_ID, Selection) |>
mutate(event_ab = rep(c("a","b"), each = n())
)
?lag
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times in selection table
filter(seq_ID != "NA_NA") |>
# group by sequence ID to create alternating sequence by selection
group_by(seq_ID) |>
mutate(sel_lag = lag(Selection))
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times in selection table
filter(seq_ID != "NA_NA") |>
# group by sequence ID to create alternating sequence by selection
group_by(seq_ID) |>
mutate(sel_lag = Selection - lag(Selection))
selns_data <- selns_int_og |>
mutate(datetime = as_datetime(`Begin Date Time`),
# assign alternating event codes
event_ab = rep_len(c("a","b"), length.out = n()))
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,event_ab, Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times included in selection table
filter(seq_ID != "NA_NA") |>
# group by sequence ID to create alternating sequence by selection
group_by(seq_ID) |>
# flag selection changes
mutate(sel_lag = Selection - lag(Selection))
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,event_ab, Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times included in selection table
filter(seq_ID != "NA_NA")
selns_seq <- intervals_cat |>
# get rid of rows without selection info
filter(!is.na(Selection),
seq_ID != "NA_NA") |>
# only keep columns needed for sequence analysis
select(-starts_with("TOL"),
-starts_with("OL"),
-Notes)
View(selns_seq)
selns_seq <- intervals_cat |>
# get rid of rows without selection info
filter(!is.na(Selection),
seq_ID != "NA_NA") |>
# only keep columns needed for sequence analysis
select(-starts_with("TOL"),
-starts_with("OL"),
-Notes) |>
# create time index variable
mutate(time_idx = seq(1:n()))
selns_seq <- intervals_cat |>
# get rid of rows without selection info
filter(!is.na(Selection),
seq_ID != "NA_NA") |>
# only keep columns needed for sequence analysis
select(seq_ID,
event_ab) |>
# create time index variable
mutate(time_idx = seq(1:n()))
View(selns_seq)
selns_seq <- intervals_cat |>
# get rid of rows without selection info
filter(!is.na(Selection),
seq_ID != "NA_NA") |>
# only keep columns needed for sequence analysis
select(seq_ID,
event_ab) |>
# create time index variable
group_by(seq_ID) |>
mutate(time_idx = seq(1:n()))
?ends_with
grepl("dredge", selns_seq$seq_ID)
fish_states <- selns_seq |>
select(seq_ID) |>
mutate(case_when(grepl("dredge", seq_ID)==TRUE ~ "fishing",
TRUE ~ "transit"))
fish_states <- selns_seq |>
select(seq_ID) |>
mutate(fish_state = case_when(grepl("dredge", seq_ID)==TRUE ~ "fishing",
TRUE ~ "transit"))
View(fish_states)
length(unique(selns_seq$seq_ID))
length(unique(fish_states$seq_ID))
event_summ <- selns_seq |>
group_by(seq_ID) |>
summarize(count = n())
View(event_summ)
?describe
event_summ <- selns_seq |>
group_by(seq_ID) |>
summarize(n_events = n())
event_summ <- selns_seq |>
group_by(seq_ID) |>
summarize(n_events = n()) |>
summarize(mean_n = mean(n_events),
med_n = median(n_events),
min_n = min(n_eventS),
max_n = max(n_events))
event_summ <- selns_seq |>
group_by(seq_ID) |>
summarize(n_events = n()) |>
ungroup()|>
summarize(mean_n = mean(n_events),
med_n = median(n_events),
min_n = min(n_eventS),
max_n = max(n_events))
event_summ <- selns_seq |>
group_by(seq_ID) |>
summarize(n_events = n())
event_summ <- selns_seq |>
group_by(seq_ID) |>
summarize(n_events = n()) |>
mutate(mean_n = mean(n_events),
med_n = median(n_events),
min_n = min(n_eventS),
max_n = max(n_events))
event_summ <- selns_seq |>
group_by(seq_ID) |>
summarize(n_events = n()) |>
summarize(mean_n = mean(n_events),
med_n = median(n_events),
min_n = min(n_events),
max_n = max(n_events))
View(event_summ)
event_summ
behav_table <- table(selns_seq$event_ab)
behav_table
behav_table <- table(selns_seq$seq_ID)
behav_table
View(behav_table)
behav_table <- table(selns_seq$event_ab) |>
arrange(Frequency)
behav_table <- table(selns_seq$event_ab) |>
as_tibble()|>
arrange(Frequency)
install.packages(c("TraMineR", "TraMineRextras", "psych"))
knitr::opts_chunk$set(echo = TRUE)
# Load libraries
library(tidyverse)
library(cluster)
library(dendextend)
library(fpc)
library(psych)
library(TraMineR)
library(TraMineRextras)
selns_seq_wide <- selns_seq |>
pivot_wider()
selns_seq_wide <- selns_seq |>
pivot_wider(values_from = seq_ID)
selns_seq_wide <- selns_seq
View(selns_seq_wide)
selns_seq_wide <- selns_seq |>
pivot_wider(values_from = event_ab)
knitr::opts_chunk$set(echo = TRUE)
# Load libraries
library(tidyverse)
library(cluster)
library(dendextend)
library(fpc)
library(psych)
library(TraMineR)
library(TraMineRextras)
# Amplitude metrics (BB SPL, OL, TOL) at 1-second resolution with vessel behavior
metrics_og <- read_csv("data/Sound_metrics/OLE_202209_coords_metrics.csv")
# fine scale selections of each change in amplitude within vessel passages
selns_int_og <- read_delim("data/Raven_selections/6125.220928161024.Table.1.selections_intervals_JM.txt")
plot(selns_int_og$Selection, selns_int_og$`Dur 90% (s)`)
hist(selns_int_og$`Dur 90% (s)`, breaks = 100)
selns_data <- selns_int_og |>
mutate(datetime = as_datetime(`Begin Date Time`),
# assign alternating event codes
event_ab = rep_len(c("a","b"), length.out = n()))
# map categories from metrics df onto selections
intervals_cat <- selns_data |>
# join dfs together
full_join(metrics_og) |>
# sort by datetime
arrange(datetime)|>
# fill in data from metrics df into selection rows
# fill selection number down to assign metrics rows to selections
fill(c(Selection,event_ab, Line_num, activity,lat,lon, GPS_speed_kt, Dist_slant))|>
# create sequence identifier of line + activity
mutate(seq_ID =
case_when(activity == "Off_eff_transit" ~ activity,
TRUE ~ paste(Line_num, activity, sep = "_"))) |>
# filter to just get times included in selection table
filter(seq_ID != "NA_NA")
selns_seq <- intervals_cat |>
# get rid of rows without selection info
filter(!is.na(Selection),
seq_ID != "NA_NA") |>
# only keep columns needed for sequence analysis
select(seq_ID,
event_ab) |>
# create time index variable
group_by(seq_ID) |>
mutate(time_idx = seq(1:n()))
# create key for sequence IDs to fishing vs. transit
fish_states <- selns_seq |>
select(seq_ID) |>
mutate(fish_state = case_when(grepl("dredge", seq_ID)==TRUE ~ "fishing",
TRUE ~ "transit"))
length(unique(selns_seq$seq_ID))
#  [1] 20
length(unique(fish_states$seq_ID))
#  [1] 20
```
event_summ <- selns_seq |>
group_by(seq_ID) |>
summarize(n_events = n()) |>
summarize(mean_n = mean(n_events),
med_n = median(n_events),
min_n = min(n_events),
max_n = max(n_events))
event_summ
behav_table <- table(selns_seq$event_ab)
selns_seq_wide
selns_seq_wide <- selns_seq |>
pivot_wider(names_from = time_idx,
values_from = event_ab)
View(selns_seq_wide)
# this contains categories that appear in the dataset
event_alphabet <- c("a","b")
# formally assigning labels which might be helpful later
event_labels <- c("a", "b")
viridis::rocket(n = 2)
# define sequence using seqdef from TraMineR package
event_seq <- seqdef(selns_seq_wide,
var = 2:322,
alphabet = event_alphabet,
labels = event_labels,
xtstep = 5,
cpal = event_colors)
event_colors <- viridis::rocket(n = 2)
# define sequence using seqdef from TraMineR package
event_seq <- seqdef(selns_seq_wide,
var = 2:322,
alphabet = event_alphabet,
labels = event_labels,
xtstep = 5,
cpal = event_colors)
seqIplot(event_seq)
View(selns_seq)
selns_seq_wide <- selns_seq |>
filter(seq_ID != "Off_eff_transit") |>
pivot_wider(names_from = time_idx,
values_from = event_ab)
# define sequence using seqdef from TraMineR package
event_seq <- seqdef(selns_seq_wide,
var = 2:82,
alphabet = event_alphabet,
labels = event_labels,
xtstep = 5,
cpal = event_colors)
seqIplot(event_seq)
costmatrix <- seqsubm(selns_seq_wide,
method="CONSTANT",
cval = 2,
with.missing=TRUE,
miss.cost=1,
time.varying=FALSE,
weighted=TRUE,
transition="both",
lag=1)
costmatrix <- seqsubm(event_seq,
method="CONSTANT",
cval = 2,
with.missing=TRUE,
miss.cost=1,
time.varying=FALSE,
weighted=TRUE,
transition="both",
lag=1)
costmatrix <- seqsubm(event_seq,
method="CONSTANT",
cval = 2,
with.missing=FALSE,
miss.cost=1,
time.varying=FALSE,
weighted=TRUE,
transition="both",
lag=1)
costmatrix
costmatrix
costmatrix <- seqsubm(event_seq,
method="CONSTANT",
cval = 2,
with.missing=TRUE,
miss.cost=1,
time.varying=FALSE,
weighted=TRUE,
transition="both",
lag=1)
costmatrix
event_seq
dist_om <- seqdist(event_seq,
method = "OM",
indel = 1.0, # insertion/,nkmjjjjmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmm
sm = costmatrix, # sub matrix = cost matrix
with.missing = TRUE)
dist_om[1:10, 1:10]
event_clusters <- agnes(dist_om, diss = TRUE, method = "ward")
plot(event_clusters, which.plot = 2)
event_clust_cut <- cutree(event_clusters, k = 2)
event_clust_cut <- cutree(event_clusters, k = 2) |>
as_factor(labels = paste("Group", 1:2))
event_clust_cut <- cutree(event_clusters, k = 2) |>
factor(labels = paste("Group", 1:2))
seqplot(event_seq,
group = event_clust_cut, type = "I")
View(event_clusters)
View(event_seq)
